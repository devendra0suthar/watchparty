{
  "name": "watchparty-socket-server",
  "version": "1.0.0",
  "description": "Socket.io server for WatchParty real-time features",
  "main": "socket.js",
  "scripts": {
    "start": "node socket.js",
    "build": "npx tsc socket.ts --outDir . --esModuleInterop --skipLibCheck"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0",
    "socket.io": "^4.8.3",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
